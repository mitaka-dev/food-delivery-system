<?xml version="1.0" encoding="UTF-8"?>
<!--
  CodeArtifact mirror settings — used in CI.
  Required env vars:
    CODEARTIFACT_TOKEN   - from `aws codeartifact get-authorization-token`
    CODEARTIFACT_URL     - e.g. https://{domain}-{account}.d.codeartifact.{region}.amazonaws.com/maven/{repo}/
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">

    <servers>
        <server>
            <id>codeartifact</id>
            <username>aws</username>
            <password>${env.CODEARTIFACT_TOKEN}</password>
        </server>
    </servers>

    <mirrors>
        <mirror>
            <id>codeartifact</id>
            <name>CodeArtifact internal repo</name>
            <url>${env.CODEARTIFACT_URL}</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
    </mirrors>

    <profiles>
        <profile>
            <id>codeartifact</id>
            <repositories>
                <repository>
                    <id>codeartifact</id>
                    <url>${env.CODEARTIFACT_URL}</url>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <activeProfiles>
        <activeProfile>codeartifact</activeProfile>
    </activeProfiles>
</settings>
